# Observe project layout 

## Scala/main.scala

Your application's entry point called Request Router, it handles all incoming HTTP requests, matching them against routes: the first route that matches request is executed;

## WEB-INF/web.xml

The Java EE deployment descriptor, an XML configuration file that specifies, how your application will be deployed into Servlet Container. By default it just maps all URLs to Circumflex Filter.