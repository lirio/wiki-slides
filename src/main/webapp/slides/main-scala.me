# Observe project layout

## Scala/main.scala

Your application's entry point called Request Router, it handles all incoming HTTP requests, matching them against routes: the first route that matches request is executed;
